{"table_name":"album_summary",
 "table_type":"view",
 "replace_query" : "select album,avg(duration) as avg_duration,max(duration) as longest_song,min(duration) as shortest_song,sum(case when explicit is False then 0 else 1 end), max(track_number) as record_count from tracks group by 1;",
 "database": "test_tables"}
  
