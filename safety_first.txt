[{"defined": " SELECT a.id,\n    a.name,\n    a.followers,\n    max(al.marketsize) AS largest_market,\n    count(DISTINCT al.id) AS albums,\n    avg(als.longest_song) AS avg_longest_song\n   FROM ((artists a\n     JOIN albums al ON ((a.id = al.artists)))\n     JOIN album_summary als ON ((al.id = als.album)))\n  GROUP BY a.id, a.name, a.followers;", "child_type": "view", "given_name": "artist_summary"}]